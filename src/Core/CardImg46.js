import { CardMedia } from "@material-ui/core";
import { withStyles } from "@material-ui/core";

export const CardImg46 = withStyles({
  root: {
    height: 0,
    paddingTop: "calc(100% * 4 / 6)",
    cursor: "pointer"
  }
})(CardMedia);
